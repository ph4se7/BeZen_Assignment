install jupyter notebook
(pip install notebook)

run jupyter notebook
(jupyter notebook)

open the file in the zip from the specified path on your PC.

install pandas
(pip install pandas)

make sure to keep the .csv data file in the same directory.

Jupyter notebook takes line by line input so make sure every line is in a new cell.
